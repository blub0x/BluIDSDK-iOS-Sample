//
//  ViewController.swift
//  BluIDSDK Sample App
//
//  Created by Akhil Kumar on 08/04/21.
//

import UIKit
import BluIDSDK
import CoreBluetooth

class ViewController: UIViewController {
    let serviceIdentifier = "42B3CF58-83EA-4CD7-9C5A-072BC0F5EBEC"
    let characteristicIdentifier = "54ECC156-1218-4603-9CDE-1029D46A886E"
    let textFile = "if flutter we can have common code everything    and while doing ml analysis it will change as per platform    if we use mlkit then flutter has official support for this    we can write bridge for anything platform specific and not available readymade    flowe flutter app: whole code will same    and we can write platform thing and later plug into flutter    how is it good?    less time to maintain ui code   performance: obviously its not native by src, but almost same as promised    anyway, it shouldn’t have been any afkggl "
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view.
    }
    
    
}


